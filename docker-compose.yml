services:
  matrix-tiktok:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - tiktok-data:/data
    restart: unless-stopped
    stop_grace_period: 30s

volumes:
  tiktok-data:
